services:
  - type: web
    name: saree-customs
    runtime: node
    plan: free
    buildCommand: npm ci --include=dev && npm run build:full
    startCommand: npx serve -s dist/public -l 3000
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
